import { LazyLoadSectionDirective } from './lazy-load-section.directive';

describe('LazyLoadSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadSectionDirective();
    expect(directive).toBeTruthy();
  });
});
